{
  "$schema": "https://raw.githubusercontent.com/denosaurs/denon/main/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run --unstable --no-check index.js",
      "desc": "",
      "allow": ["env", "write", "read", "net", "run", "plugin"]
    }
  }
}
